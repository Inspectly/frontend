{
  "name": "@inspectly/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.5.0",
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-redux": ">=9.0.0"
  }
}
